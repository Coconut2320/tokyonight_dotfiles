if status is-interactive
    # Commands to run in interactive sessions can go here
end

# User-added below
fastfetch
alias cls="clear && ls"
alias ff="fastfetch"
alias iwctl="iwctl && ping archlinux.org"
